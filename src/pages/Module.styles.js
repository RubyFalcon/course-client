import styled from "@emotion/styled/macro";
import { Text, Badge } from "@chakra-ui/react";

export const TextData = styled(Text)`
  margin-top: 20px;
  font-size: 18px;
  font-weight: 600;
  font-style: italic;
`;

export const CourseName = styled(Badge)`
  width: fit-content;
`;
